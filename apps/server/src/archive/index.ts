// Archive: every chunk persisted to Cloudflare R2 (S3 API, free egress).
// Source of truth. Handles reconciliation in both directions:
// "send me ≥ seq N" toward phones, trickle-up from the desk after LAN takes.
